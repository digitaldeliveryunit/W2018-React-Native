import { StyleSheet } from "react-native";

export default StyleSheet.create({
  listContainer: {
    flex: 1
  },
  itemWrapper: {
    marginBottom: 20,
    paddingLeft: 15,
    paddingRight: 15
  }
});
